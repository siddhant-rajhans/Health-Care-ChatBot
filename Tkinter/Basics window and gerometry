from tkinter import *
# created a instance from TK class 
main_root = Tk()


#geometry parameters (width x height)
main_root.geometry("500x500")

#lock minimum window size (width , height )
main_root.minsize(200,200)

#lock maximum window size (width , height )
main_root.maxsize(600,600)




main_root.mainloop()
